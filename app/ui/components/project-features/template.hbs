<div class="project-features__title">
	<h2>
		<span>{{data.titleBefore}}</span>
		<span class="color--{{meta.id}}">{{data.titleColored}}</span>
	</h2>
</div>

{{#each data.features as |feature|}}
	{{project-features-item data=feature selected=selected}}
{{/each}}

{{yield}}