{{#each items as |item|}}
	{{#link-to 'index'
		invokeAction=(action 'onItemSelected' item)
		class=(if (equals item selected) 'menu-strip__link menu-strip__link__active' 'menu-strip__link')
		}}
		{{item.id}}
	{{/link-to}}
{{/each}}