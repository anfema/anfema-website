{{#each data.team as |content|}}
	{{!-- TODO: Make query param dynamic, will probably need a custom helper or better a subclass --}}
	{{content-folder-item content=content selected=selected}}
{{/each}}
