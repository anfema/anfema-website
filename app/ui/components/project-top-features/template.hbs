<div class="project-topFeatures__text">
    <h2>
        <span>{{data.titleBefore}}</span>
        <span>{{data.titleColored}}</span>
    </h2>

    {{#each data.topFeatures as |feature|}}
        {{project-topFeatures-item data=feature selected=selected}}
    {{/each}}
</div>

{{yield}}