{{#each images as |image|}}
	{{image-srcset
		defaults=imageSrcsetDefaults
		image=image
		class='content-project-gallery__image'
		width=defaults.width
		height=defaults.height
	}}
{{/each}}

{{yield}}