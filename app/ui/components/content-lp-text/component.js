import Component from '@ember/component';

export default Component.extend({
	classNames: ['lp-section'],
	tagName: 'section',

	anchor_id: null,
	title: null,
	content: null,
	section_number: null,
});
