import Component from '@ember/component';

export default Component.extend({
	classNames: ['content-lp-section'],
	tagName: 'section',

	anchorId: null,
	title: null,
	content: null,
	sectionLabel: null,
});
